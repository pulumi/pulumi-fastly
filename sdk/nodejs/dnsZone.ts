// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides a Fastly DNS Zone.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as fastly from "@pulumi/fastly";
 *
 * const example = new fastly.DnsZone("example", {
 *     name: "example.com.",
 *     description: "My secondary DNS zone",
 *     xfrConfigInbound: {
 *         inboundTsigKeyId: exampleFastlyTsigKey.id,
 *         primaries: [
 *             {
 *                 address: "192.0.2.1",
 *                 description: "Primary nameserver #1",
 *             },
 *             {
 *                 address: "192.0.2.2",
 *                 description: "Primary nameserver #1",
 *             },
 *         ],
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * Fastly DNS Zones can be imported using their Zone ID, e.g.
 *
 * ```sh
 * $ pulumi import fastly:index/dnsZone:DnsZone example xxxxxxxxxxxxxxxxxxxx
 * ```
 */
export class DnsZone extends pulumi.CustomResource {
    /**
     * Get an existing DnsZone resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DnsZoneState, opts?: pulumi.CustomResourceOptions): DnsZone {
        return new DnsZone(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'fastly:index/dnsZone:DnsZone';

    /**
     * Returns true if the given object is an instance of DnsZone.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DnsZone {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DnsZone.__pulumiType;
    }

    /**
     * A freeform descriptive note.
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * The domain name for your zone in FQDN format (e.g. `example.com.`). Must include a trailing dot.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * All attributes associated with inbound zone transfers.
     */
    declare public readonly xfrConfigInbound: pulumi.Output<outputs.DnsZoneXfrConfigInbound | undefined>;

    /**
     * Create a DnsZone resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: DnsZoneArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DnsZoneArgs | DnsZoneState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DnsZoneState | undefined;
            resourceInputs["description"] = state?.description;
            resourceInputs["name"] = state?.name;
            resourceInputs["xfrConfigInbound"] = state?.xfrConfigInbound;
        } else {
            const args = argsOrState as DnsZoneArgs | undefined;
            resourceInputs["description"] = args?.description;
            resourceInputs["name"] = args?.name;
            resourceInputs["xfrConfigInbound"] = args?.xfrConfigInbound;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DnsZone.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DnsZone resources.
 */
export interface DnsZoneState {
    /**
     * A freeform descriptive note.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * The domain name for your zone in FQDN format (e.g. `example.com.`). Must include a trailing dot.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * All attributes associated with inbound zone transfers.
     */
    xfrConfigInbound?: pulumi.Input<inputs.DnsZoneXfrConfigInbound | undefined>;
}

/**
 * The set of arguments for constructing a DnsZone resource.
 */
export interface DnsZoneArgs {
    /**
     * A freeform descriptive note.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * The domain name for your zone in FQDN format (e.g. `example.com.`). Must include a trailing dot.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * All attributes associated with inbound zone transfers.
     */
    xfrConfigInbound?: pulumi.Input<inputs.DnsZoneXfrConfigInbound | undefined>;
}
